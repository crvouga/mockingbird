{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/packages/service/*/README.md",
        "$TURBO_ROOT$/packages/service/*/SUPPORT.md",
        "$TURBO_ROOT$/packages/service/*/package.json"
      ],
      "outputs": ["dist/**"]
    }
  }
}
