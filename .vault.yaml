# Vault (OpenBao) coordinates — no secrets. Shared infra contract:
# https://raw.githubusercontent.com/crvouga/workspace/main/INTEGRATING.md
#
# `bun scripts/vault-run.ts` (every root turbo script) wraps commands in `vault run`, which reads
# this file and injects secret/personal/<config> as env vars: the Turborepo remote cache
# (TURBO_*) in every config, live-parity sandbox keys (MOCKINGBIRD_*) in prd.
# Also used by: bun run vault:login | secrets:doctor | secrets:check | secrets:sync
addr: https://vault.chrisvouga.dev
mount: secret
project: personal
config: dev
