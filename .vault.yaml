# HashiCorp Vault connection for maintainer secret tooling.
# Used by: bun run secrets:doctor | secrets:check | secrets:sync
# See docs/SECRETS.md
#
# Live parity sandbox keys live under secret/data/mockingbird/<provider>
# (see @crvouga/mockingbird-openbao). Publish uses npm OIDC, not Vault.
addr: https://vault.chrisvouga.dev
mount: secret
project: personal
config: prd
