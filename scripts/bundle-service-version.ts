/**
 * The version every service bundle reports until `release:publish` stamps the one it
 * publishes. Must equal UNRELEASED_VERSION in packages/service/core/src/version.ts.
 */
export const VERSION_PLACEHOLDER = "0.0.0-development"
